# Signal has no required environment variables for the demo build —
# all data is local mock data. This file documents where real keys
# would go if wired up to a live API.
NEXT_PUBLIC_APP_URL=http://localhost:3000
